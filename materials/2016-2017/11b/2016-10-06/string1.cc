#include <iostream>
#include <string>

using namespace std;

int main() {
    string str1 = "Hello, C++ string!";
    cout << str1 << endl;
    
    // reading from console example
    string input;
    // reads just one word
    cin >> input;
    cout << "Hello, " << input << "!" << endl;  

    return 0;
}
