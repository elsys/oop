package test.food;

// import org.elsys.squirrels.food.BaseFood;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class BaseFoodTest {
    @Test
    void getFoodScore() {
        // BaseFood food = new BaseFood(10, 10);
        // assertEquals(food.getFoodScore(), 100);
        //
        // food = new BaseFood(20, 10);
        // assertEquals(food.getFoodScore(), 200);
        //
        // food = new BaseFood(10, 20);
        // assertEquals(food.getFoodScore(), 200);
    }
}
